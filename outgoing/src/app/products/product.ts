export class Product {
    name: string;
    buy: number;
    sell: number;
    des: string;

    constructor(name: string, buy: number, sell: number, des: string) {
        this.name = name;
        this.buy = buy;
        this.sell = sell;
        this.des = des;
    }
}
